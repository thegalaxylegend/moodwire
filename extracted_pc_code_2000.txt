"# Implementation Plan: Restoring 3D Interactive Particle Backdrop & Page Entrance Animations\n\nThis plan restores the high-fidelity 3D interactive particle cloud backdrop and fixes entrance/scroll animation conflicts on the **Founder Page** (`/founder`).\n\n---\n\n## User Review Required\n\nPlease review the proposed architectural and animation details:\n> [!IMPORTANT]\n> - **3D Interactive Particle Field**: We will restore the 3D particle background inside a WebGL `<Canvas>` powered by `@react-three/fiber` and `@react-three/drei`. The cloud will distribute particles spherically (with organic orbital bands) and dynamically sway/rotate based on both mouse movements and page scrolling.\n> - **Performance & SSR Protection**: \n>   - We will protect the page from Hydration Mismatches and SSR execution errors by ensuring the Canvas component only instantiates on the client side after mounting (`mounted === true`).\n>   - R3F particle count will scale down or turn off entirely for users on lower-performance devices (`tier === 'low'`) to maintain 60fps+.\n> - **Jitter-Free Mount Fade-in**: We will resolve the clash in the Hero section where Framer Motion variants for `opacity` were being overridden by the scroll-linked `MotionValue` (`smoothOpacity`), causing elements to snap into view. We'll adjust `HeroScrollSection` so page load animations run first, followed by scroll parallax tracking.\n\n---\n\n## Proposed Changes\n\n### Component: FounderPage\n#### [MODIFY] [FounderPage.tsx](file:///c:/Users/Admin/Downloads/Desktop/src/pages/public/FounderPage.tsx)\n\nWe will modify [FounderPage.tsx](file:///c:/Users/Admin/Downloads/Desktop/src/pages/public/FounderPage.tsx) to:\n1. Re-add R3F imports: `Canvas`, `useFrame` from `@react-three/fiber`, `PointMaterial` from `@react-three/drei`, and `THREE` from `three`.\n2. Add the `ParticleCloud` functional component with a double-helix/orbital distribution algorithm.\n3. Integrate mouse movement sways and scroll-based rotation velocities into the `useFrame` render loop.\n4. Add cl
<truncated 620 bytes>