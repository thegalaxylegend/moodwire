1. Ensure the offline capabilities are functional.
   - I already added `enableMultiTabIndexedDbPersistence` to `src/lib/firebase.ts`. This takes care of requirements 1 and 4.
   - Requirement 2 is natively handled by Firebase Auth and Firestore sockets.
   - Requirement 3 is inherently supported by the React Suspense architecture and lazy loading seen in `App.tsx`.
   - Requirement 5: I've checked `MockExamEngine.tsx` and it has a "Retry" button that renders if questions fail to load. `MockGenerator` also has auto-retries. This handles timeouts via UI fallbacks instead of white screens.
   - Requirement 6: TTS uses local WASM in `sherpa-worker.ts`.

2. Call `request_plan_review`.
3. Set the plan using `set_plan`.
4. Perform pre-commit hooks (`pre_commit_instructions`).
5. Write the final QA output response.
