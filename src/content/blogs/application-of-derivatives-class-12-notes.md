---
heroImage: "/blog-images/application-of-derivatives-class-12-notes.webp"
title: "Application Of Derivatives Class 12 Exam Prep Revision — Grandmaster Guide"
description: "Application Of Derivatives Class 12 Exam Prep Revision — Grandmaster Guide Revision Notes. Last Updated: 2026-04-20."
category: "Exam Notes"
date: "2026-04-20"
practice_link: "/practice/application-of-derivatives-class-12-notes"
manualReview: false
---

## ⚡ Formula Bank
- The derivative of a function $f(x)$ is denoted as $\frac{d}{dx}f(x)$ or $f'(x)$ and represents the rate of change of the function with respect to $x$.
- The geometric interpretation of a derivative is the slope of the tangent line to the graph of $f(x)$ at a point $x$.
- The equation of the tangent line to the graph of $f(x)$ at a point $(a, f(a))$ is given by $y - f(a) = f'(a)(x - a)$.
- The derivative of a function $f(x)$ is used to find the maximum and minimum values of the function, and is given by $\frac{d}{dx}f(x) = 0$.
- The first derivative test is used to determine the nature of the critical points of a function, and states that if $f'(x) > 0$ for $x < a$ and $f'(x) < 0$ for $x > a$, then $f(x)$ has a local maximum at $x = a$.
- The second derivative test is used to determine the nature of the critical points of a function, and states that if $f''(x) > 0$ for all $x$, then $f(x)$ is concave upwards, and if $f''(x) < 0$ for all $x$, then $f(x)$ is concave downwards.
- The derivative of a function $f(x)$ can be used to find the approximate value of the function at a point $x = a + \Delta x$, and is given by $f(a + \Delta x) \approx f(a) + f'(a)\Delta x$.

## 
- Not using the chain rule correctly, which states that if $f(x) = g(h(x))$, then $f'(x) = g'(h(x)) \cdot h'(x)$.
- Not using the product rule correctly, which states that if $f(x) = g(x) \cdot h(x)$, then $f'(x) = g'(x) \cdot h(x) + g(x) \cdot h'(x)$.
- Not using the quotient rule correctly, which states that if $f(x) = \frac{g(x)}{h(x)}$, then $f'(x) = \frac{h(x) \cdot g'(x) - g(x) \cdot h'(x)}{(h(x))^2}$.
- Not checking the domain of the function before differentiating, which can lead to incorrect results.
- Not using the correct units when applying derivatives to physical problems, which can lead to incorrect results.

## ✏️ 3 Solved PYQs
- **Question 1:** Find the derivative of the function $f(x) = \frac{x^2 \sin x}{\cos x}$.
- To find the derivative, we can use the quotient rule, which states that if $f(x) = \frac{g(x)}{h(x)}$, then $f'(x) = \frac{h(x) \cdot g'(x) - g(x) \cdot h'(x)}{(h(x))^2}$.
- Let $g(x) = x^2 \sin x$ and $h(x) = \cos x$, then $g'(x) = 2x \sin x + x^2 \cos x$ and $h'(x) = -\sin x$.
- Substituting these values into the quotient rule formula, we get $f'(x) = \frac{\cos x (2x \sin x + x^2 \cos x) - x^2 \sin x (-\sin x)}{(\cos x)^2}$.
- Simplifying this expression, we get $f'(x) = \frac{2x \sin x \cos x + x^2 \cos^2 x + x^2 \sin^2 x}{\cos^2 x}$.
- **Question 2:** Find the equation of the tangent line to the graph of $f(x) = x^2$ at the point $(1, 1)$.
- To find the equation of the tangent line, we need to find the slope of the tangent line, which is given by the derivative of the function at the point $(1, 1)$.
- The derivative of $f(x) = x^2$ is $f'(x) = 2x$, so the slope of the tangent line at the point $(1, 1)$ is $f'(1) = 2$.
- The equation of the tangent line is given by $y - f(1) = f'(1)(x - 1)$, which simplifies to $y - 1 = 2(x - 1)$.
- **Question 3:** Find the maximum value of the function $f(x) = x^3 - 6x^2 + 9x + 2$.
- To find the maximum value, we need to find the critical points of the function, which are given by the values of $x$ for which $f'(x) = 0$.
- The derivative of $f(x) = x^3 - 6x^2 + 9x + 2$ is $f'(x) = 3x^2 - 12x + 9$, so the critical points are given by $3x^2 - 12x + 9 = 0$.
- Solving this equation, we get $x = 1$ and $x = 3$, so the critical points are $(1, f(1))$ and $(3, f(3))$.
- To determine which of these points corresponds to a maximum, we can use the second derivative test, which states that if $f''(x) > 0$ for all $x$, then $f(x)$ is concave upwards, and if $f''(x) < 0$ for all $x$, then $f(x)$ is concave downwards.
- The second derivative of $f(x) = x^3 - 6x^2 + 9x + 2$ is $f''(x) = 6x - 12$, so $f''(1) = -6$ and $f''(3) = 6$.
- Since $f''(1) < 0$, the point $(1, f(1))$ corresponds to a local maximum, and since $f''(3) > 0$, the point $(3, f(3))$ corresponds to a local minimum.

## 🧠 The One Thing Most Students Get Wrong
- The one thing that most students get wrong when applying derivatives is not checking the domain of the function before differentiating.
- This can lead to incorrect results, as the derivative of a function is only defined for values of $x$ that are in the domain of the function.
- For example, the function $f(x) = \frac{1}{x}$ is not defined at $x = 0$, so the derivative $f'(x) = -\frac{1}{x^2}$ is also not defined at $x = 0$.
- Therefore, it is essential to check the domain of the function before differentiating to ensure that the derivative is defined for all values of $x$.

## 👁️ Ayush's Note
- When applying derivatives, it is essential to use the correct units and to check the domain of the function before differentiating.
- The derivative of a function can be used to find the maximum and minimum values of the function, and to determine the nature of the critical points.
- The first derivative test and the second derivative test can be used to determine the nature of the critical points, and to find the maximum and minimum values of the function.
- The derivative of a function can also be used to find the approximate value of the function at a point $x = a + \Delta x$, and to find the equation of the tangent line to the graph of the function at a point $(a, f(a))$.

## 🔁 Last 5 Minutes Box
- The derivative of a function $f(x)$ is denoted as $\frac{d}{dx}f(x)$ or $f'(x)$ and represents the rate of change of the function with respect to $x$.
- The geometric interpretation of a derivative is the slope of the tangent line to the graph of $f(x)$ at a point $x$.
- The equation of the tangent line to the graph of $f(x)$ at a point $(a, f(a))$ is given by $y - f(a) = f'(a)(x - a)$.
- The derivative of a function $f(x)$ can be used to find the maximum and minimum values of the function, and to determine the nature of the critical points.
- The first derivative test and the second derivative test can be used to determine the nature of the critical points, and to find the maximum and minimum values of the function.

## 📝 Practice MCQs
**1. Question:** Find the derivative of the function $f(x) = \frac{x^2}{\sin x}$.
- A) $\frac{2x \sin x - x^2 \cos x}{\sin^2 x}$
- B) $\frac{2x \sin x + x^2 \cos x}{\sin^2 x}$
- C) $\frac{2x \cos x - x^2 \sin x}{\cos^2 x}$
- D) $\frac{2x \cos x + x^2 \sin x}{\cos^2 x}$
**Answer:** A) $\frac{2x \sin x - x^2 \cos x}{\sin^2 x}$.
**2. Question:** Find the equation of the tangent line to the graph of $f(x) = x^2$ at the point $(2, 4)$.
- A) $y - 4 = 4(x - 2)$
- B) $y - 4 = 2(x - 2)$
- C) $y - 4 = x - 2$
- D) $y - 4 = 2x - 2$
**Answer:** A) $y - 4 = 4(x - 2)$.
**3. Question:** Find the maximum value of the function $f(x) = x^3 - 3x^2 + 2$.
- A) $f(1) = 0$
- B) $f(2) = -2$
- C) $f(0) = 2$
- D) $f(3) = 2$
**Answer:** A) $f(1) = 0$.
**4. Question:** Find the derivative of the function $f(x) = \sqrt{x}$.
- A) $\frac{1}{2\sqrt{x}}$
- B) $\frac{1}{\sqrt{x}}$
- C) $\frac{1}{2x}$
- D) $\frac{1}{x}$
**Answer:** A) $\frac{1}{2\sqrt{x}}$.
**5. Question:** Find the equation of the tangent line to the graph of $f(x) = x^2$ at the point $(1, 1)$.
- A) $y - 1 = 2(x - 1)$
- B) $y - 1 = x - 1$
- C) $y - 1 = 2x - 2$
- D) $y - 1 = x$
**Answer:** A) $y - 1 = 2(x - 1)$.

---

### 🚀 Ready to Ace Your Exam?
Put your knowledge to the test! Take the free [**Practice Mock Test**](/practice/application-of-derivatives-class-12-notes) now and track your progress against thousands of students.

---
*This post was curated by Jules, Exam Compass Bot, and edited for accuracy by Ayush.*
